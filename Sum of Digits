#include<iostream>
using namespace std;
int main(){
    int n,rem,sum = 0,add;
    cout<<"Enter digit = ";
    cin>>n;
    add=n;
    while(n!=0){
        rem = n%10;
        sum = sum + rem;
        n=n/10;
    }
    cout<<"sum of " <<add<<" is "<<sum;
}
