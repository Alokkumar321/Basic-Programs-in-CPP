#include <stdio.h>

int main()
{
    int arr[10],i;
    printf("Input 10 Elements\n");
    for(i=0;i<10;i++)
    {
        printf("Element %d is ",i);
        scanf("%d",&arr[i]);
    }
    printf("Elements are =");
    for(i=0;i<10;i++)
    {
        printf("%d ",arr[i]);
    }
    return 0;
}
