#include<iostream>
#include<math.h>
using namespace std;
int main(){
    int i,rem,n,sum=0;
    cout<<"Enter a Binary = ";
    cin>>n;
    i=0;
    while(n>0){
        rem = n%10;
        sum = sum + rem*pow(2,i);
        n =n/10;
        i++;
    }
    cout<<sum;
    return 0;
}
